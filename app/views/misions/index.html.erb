
<p id="notice"><%= notice %></p>
<h1>Misions</h1>

<table>
  <thead>
  
    <tr>
      <th>Title</th>
      <th>Priority</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @misions.each do |mision| %>
      <tr>
        <td><%= mision. title %></td>
        <td><%= mision.priority %></td>

        <td><%= case mision.status 
        when 0 then "Open"
        when 1 then "Closed"
        when 2 then "Resolved"
            end%> </td>
        <td><%= link_to 'Show', mision %></td>
        <td><%= link_to 'Edit', edit_mision_path(mision) %></td>
        <td><%= link_to 'Destroy', mision, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @misions, renderer: BootstrapPagination::Rails %>
<br>

<%= link_to 'New Mision', new_mision_path, class: 'btn btn-default' %>

<%= link_to 'Open misions', open_path, class: 'btn btn-info' %>
<%= link_to 'Closed misions', closed_path, class: 'btn btn-info' %>
<%= link_to 'Open misions', open_path, class: 'btn btn-info' %>


